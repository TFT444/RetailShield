# Placeholder — GitHub Actions CI pipeline (lint, test, Sentinel deploy) to be implemented
name: RetailShield CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder step
        run: echo "CI pipeline not yet implemented"
